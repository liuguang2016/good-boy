const numberArray: number[] = [1, 2, 3, 4];

const arr: (string | number)[] = [1, "2", 3, "4"];

//类型别名 type alias

type Lady = { age: number, name: string };

const girls: Lady[] = [{ age: 12, name: '细节' }, { age: 21, name: "看见" }];