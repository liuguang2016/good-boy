// 基本 number string boolean null undefined symbol
const base: string = "字符串";

//数组
const datas: number[] = [1, 2, 3, 4, 5];

// 类
class Person {};

const Ming: Person = new Person();

//函数
const handleTransform = ():string => {
  return "Ming";
};